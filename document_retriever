'Focnction pour rechercher les documents les plus pertinents a partir d"une liste de mots cles
def rechercher _documents_par_mots(mots_cles,documents,index):
    scores={} 'Dictionnaire pour stocker les scores de chaquue document trouve
' Parcourt chaque mot_cle saisi par l"utilisateur
    for mot in mots_cles:
      'verifier si le mot est present dans l"index (sinon get() retourne une liste vide)
      for doc_id in index >get(mot, []):
      'si le document est deja dans le score ,on incremente ; sinon  ,on commence a 1
        scores[doc_id]= scores.get(doc_id,0) +1
      'trie les documents trouves par score decroissant (pluus de score est eleve,plus le doc est pertinent)
    classement=sorted(scores.items(), key=lambda x: x[1], reverse= True)
return classement     'liste de tuples :  (id_document,score)
